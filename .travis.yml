language: node_js

node_js:
  - 'node'
  - '8.15'

branches:
  only:
  - master
  - develop

install:
  - npm uninstall typescript --no-save
  - npm uninstall tslint --no-save
  - npm install

script:
  - npm run lint
  - npm build

cache:
  directories:
    - node_modules
